[pytest]
markers =
    slow: real-API-call test suites (Milestone 6's fixture-based extraction
        accuracy suite) that are excluded from the default fast run and
        must be invoked explicitly, e.g.:
        pytest tests/test_extraction_accuracy.py -m slow
addopts = -m "not slow"
